# 0001 — CI/CD (GitHub Actions)

**What to build:** GitHub Actions CI workflow 配置文件，自动验证 install.sh 语法、Python 脚本可导入、auto-develop.py 不 crash。

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] 创建 `.github/workflows/ci.yml`
- [ ] Shellcheck 检查 `install.sh`
- [ ] Python 语法检查：`python3 -m py_compile scripts/*.py`
- [ ] 验证 `auto-develop.py --help` 正常退出
- [ ] 验证 `auto-develop.py --status .` 正常退出
- [ ] 触发条件：push 到 main + pull request
